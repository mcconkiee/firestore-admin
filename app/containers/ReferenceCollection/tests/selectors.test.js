// import { fromJS } from 'immutable';
// import { selectReferenceCollectionDomain } from '../selectors';

describe('selectReferenceCollectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
