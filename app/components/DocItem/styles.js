import styled from 'styled-components';
import { Button } from '@material-ui/core';

export const Anchor = styled.a``;
export const FilterButton = styled(Button)``;
export const NestObject = styled.div`
  padding: 10px;
  background-color: #cacaca;
`;
